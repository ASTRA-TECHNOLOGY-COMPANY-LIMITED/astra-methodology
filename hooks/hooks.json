{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🌟 ASTRA 방법론이 활성화되어 있습니다. /astra-guide 로 빠른 참조를 확인하세요.'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(echo $CLAUDE_TOOL_INPUT | jq -r '.file_path // .filePath // empty' 2>/dev/null); if [ -n \"$FILE\" ] && echo \"$FILE\" | grep -qE '(database-design\\.md|blueprints/.*\\.md)$'; then echo '[ASTRA] 설계 문서가 수정되었습니다. VA 검토 후 코드 반영을 진행하세요.'; fi"
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/check-forbidden-words.sh",
            "timeout": 30,
            "statusMessage": "금칙어 검사 중..."
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/validate-naming.sh",
            "timeout": 30,
            "statusMessage": "네이밍 규칙 검사 중..."
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/track-sprint-progress.sh",
            "timeout": 15,
            "statusMessage": "스프린트 진행 추적 중..."
          }
        ]
      }
    ]
  }
}
